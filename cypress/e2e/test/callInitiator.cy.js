
describe('Cloud Telephony Automation', () => {
  
    before(() => {
      // Base URL is defined in cypress.config.js
      cy.visit('https://www.google.com/');
    });
  
    it('should login as test user and initiate the call @smoke', () => {
      // Enter username
      cy.log('Hello test');
        
    });
  });
  